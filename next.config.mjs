/** @type {import('next').NextConfig} */
const nextConfig = {
        images: {
                domains: ["cdn.imagin.studio","res.cloudinary.com","www.course-api.com"]
            }
        
};

export default nextConfig;
